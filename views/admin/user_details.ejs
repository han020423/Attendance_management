<!DOCTYPE html>
<html>
<head>
  <title><%= title %></title>
  <link rel="stylesheet" href="/css/style.css">
</head>
<body>
  <h1><%= userToEdit.name %> - 사용자 정보 수정</h1>
  <p><a href="/admin/users">사용자 목록으로</a></p>

  <form action="/admin/users/<%= userToEdit.id %>" method="post">
    <div>
      <label for="name">이름:</label>
      <input type="text" id="name" name="name" value="<%= userToEdit.name %>" required>
    </div>
    <div>
      <label for="email">이메일:</label>
      <input type="email" id="email" name="email" value="<%= userToEdit.email %>" required>
    </div>
    <div>
      <label for="role">역할:</label>
      <select name="role" id="role">
        <option value="STUDENT" <%= userToEdit.role === 'STUDENT' ? 'selected' : '' %>>학생</option>
        <option value="INSTRUCTOR" <%= userToEdit.role === 'INSTRUCTOR' ? 'selected' : '' %>>교원</option>
        <option value="ADMIN" <%= userToEdit.role === 'ADMIN' ? 'selected' : '' %>>관리자</option>
      </select>
    </div>
    <button type="submit">정보 업데이트</button>
  </form>

  <hr style="margin-top: 2rem; margin-bottom: 2rem;">

  <h2>사용자 삭제</h2>
  <form action="/admin/users/<%= userToEdit.id %>/delete" method="post" onsubmit="return confirm('정말로 이 사용자를 삭제하시겠습니까? 이 작업은 되돌릴 수 없습니다.');">
    <p>이 사용자를 시스템에서 영구적으로 삭제합니다. 이 작업은 되돌릴 수 없습니다.</p>
    <button type="submit" class="btn-delete">사용자 삭제</button>
  </form>

</body>
</html>
