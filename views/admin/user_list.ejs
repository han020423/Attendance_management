<!DOCTYPE html>
<html>
<head>
  <title><%= title %></title>
  <link rel="stylesheet" href="/css/style.css">
</head>
<body>
  <h1><%= title %></h1>
  <p><a href="/admin">대시보드로 돌아가기</a></p>

  <nav>
    <a href="/admin/users">사용자 관리</a> |
    <a href="/admin/semesters">학기 관리</a> |
    <a href="/admin/departments">학과 관리</a> |
    <a href="/admin/courses">과목 관리</a>
  </nav>
  <hr>

  <table>
    <thead>
      <tr>
        <th>ID</th>
        <th>이름</th>
        <th>이메일</th>
        <th>역할</th>
        <th>가입일</th>
        <th>관리</th>
      </tr>
    </thead>
    <tbody>
      <% users.forEach(user => { %>
        <tr>
          <td><%= user.id %></td>
          <td><%= user.name %></td>
          <td><%= user.email %></td>
          <td><%= user.role %></td>
          <td><%= new Date(user.createdAt).toLocaleDateString() %></td>
          <td>
            <a href="/admin/users/<%= user.id %>">수정</a>
            <form action="/admin/users/<%= user.id %>/delete" method="post" style="display: inline; margin-left: 10px;" onsubmit="return confirm('정말로 이 사용자를 삭제하시겠습니까? 이 작업은 되돌릴 수 없습니다.');">
              <button type="submit" class="btn-delete">삭제</button>
            </form>
          </td>
        </tr>
      <% }) %>
    </tbody>
  </table>

</body>
</html>
