<!DOCTYPE html>
<html>
<head>
  <title><%= title %></title>
  <link rel="stylesheet" href="/css/style.css">
</head>
<body>
  <h1><%= title %></h1>
  <p><a href="/me/dashboard">대시보드로 돌아가기</a></p>
  <a href="/messages/new">새 메시지 작성</a>

  <table>
    <thead>
      <tr>
        <th>상태</th>
        <th>보낸 사람</th>
        <th>제목</th>
        <th>받은 시간</th>
      </tr>
    </thead>
    <tbody>
      <% messages.forEach(message => { %>
        <tr style="<%= message.is_read ? '' : 'font-weight: bold;' %>">
          <td><%= message.is_read ? '읽음' : '안읽음' %></td>
          <td><%= message.FromUser ? message.FromUser.name : 'N/A' %></td>
          <td><a href="/messages/<%= message.id %>"><%= message.title %></a></td>
          <td><%= new Date(message.createdAt).toLocaleString() %></td>
        </tr>
      <% }) %>
    </tbody>
  </table>

</body>
</html>
