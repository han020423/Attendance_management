<!DOCTYPE html>
<html>
<head>
  <title>수업 세션 수정</title>
  <link rel="stylesheet" href="/css/style.css">
</head>
<body>
  <div class="container mt-4">
    <h1><%= session.week %>주차 수업 수정</h1>
    <p><strong>과목:</strong> <%= session.Course.title %></p>

    <form action="/sessions/<%= session.id %>/edit" method="POST">
      <div class="mb-3">
        <label for="date" class="form-label">수업 날짜</label>
        <input type="date" class="form-control" id="date" name="date" value="<%= session.date ? new Date(session.date).toISOString().split('T')[0] : '' %>" required>
      </div>
      <div class="mb-3">
        <label for="start_at" class="form-label">시작 시간</label>
        <input type="time" class="form-control" id="start_at" name="start_at" value="<%= session.start_at %>" required>
      </div>
      <div class="mb-3">
        <label for="end_at" class="form-label">종료 시간</label>
        <input type="time" class="form-control" id="end_at" name="end_at" value="<%= session.end_at %>" required>
      </div>
      <div class="d-flex justify-content-end">
        <a href="/courses/<%= session.course_id %>" class="btn btn-secondary me-2">취소</a>
        <button type="submit" class="btn btn-primary">저장</button>
      </div>
    </form>
  </div>
</body>
</html>
